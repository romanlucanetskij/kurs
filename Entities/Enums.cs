namespace DripCube.Entities
{

    public enum EmployeeRole
    {
        Admin = 0,
        Manager = 1
    }


    public enum ChatStatus
    {
        New = 0,
        InProgress = 1,
        Closed = 2
    }


    public enum SenderRole
    {
        User = 0,
        Manager = 1,
        System = 2
    }
}