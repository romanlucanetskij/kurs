# Инструкция по деплою DripCube на Render

Эта инструкция поможет вам развернуть ASP.NET Core приложение DripCube на платформе Render.

## Предварительные требования

- Аккаунт на [Render.com](https://render.com)
- Git репозиторий с вашим проектом (GitHub, GitLab или Bitbucket)
- База данных PostgreSQL уже настроена на Render

## Шаг 1: Подготовка кода

✅ **Уже выполнено!** Все необходимые конфигурационные файлы созданы:
- `render.yaml` - конфигурация для Render
- `.dockerignore` - исключения для деплоя
- `Program.cs` - обновлен для использования переменных окружения
- `appsettings.json` - очищен от секретных ключей

## Шаг 2: Коммит и пуш изменений в Git

```bash
git add .
git commit -m "Подготовка к деплою на Render"
git push origin main
```

> **Примечание:** Замените `main` на вашу ветку, если используете другое имя (например, `master`).

## Шаг 3: Создание Web Service на Render

1. Войдите в [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш Git репозиторий:
   - Выберите нужный репозиторий из списка
   - Если не видите репозиторий, нажмите **"Configure account"** и предоставьте доступ
4. Render автоматически обнаружит `render.yaml` и настроит сервис

## Шаг 4: Настройка переменных окружения

⚠️ **ВАЖНО!** Необходимо настроить следующие переменные окружения в Render Dashboard:

### В разделе "Environment" → "Environment Variables" добавьте:

#### 1. База данных
```
DATABASE_URL = Host=dpg-d4g6db8dl3ps73da19pg-a.oregon-postgres.render.com;Database=abcn;Username=user;Password=NBWohmR0QCiyPLqFd2uGR2HWMNvm9GnA;SslMode=Require;Trust Server Certificate=true;
```

#### 2. Cloudinary Settings
```
CLOUDINARY_CLOUD_NAME = dd8lrzw0s
CLOUDINARY_API_KEY = 839158591392738
CLOUDINARY_API_SECRET = EnC6RHDyIYorTryGnLUTrvSRnbo
```

#### 3. Stripe Settings
```
STRIPE_PUBLISHABLE_KEY = pk_test_51SXRvaRq3TM6Cq5tNBxl6UjQyoo0FqnZfNBKZb4opnC51Q12JMjX4DYH1VzyGSMRbwSO827qCv4BtRxE2vzN8D6W00TqEMEoPf
STRIPE_SECRET_KEY = sk_test_51SXRvaRq3TM6Cq5tdg8kEErnMzCdzZ69B0YyCTX3FAU9UDRFWzd4HE1GnKGFSpqhbkO79iy89LKABUX1dixt1Cm700ksXoe0YG
```

#### 4. ASP.NET Core Settings
```
ASPNETCORE_ENVIRONMENT = Production
ASPNETCORE_URLS = http://0.0.0.0:$PORT
```

> **Совет:** Переменная `$PORT` автоматически подставляется Render'ом.

## Шаг 5: Запуск деплоя

1. После настройки всех переменных, нажмите **"Create Web Service"**
2. Render автоматически начнет процесс сборки и деплоя
3. Следите за логами в реальном времени:
   - **Build Logs** - процесс компиляции
   - **Deploy Logs** - запуск приложения

### Ожидаемые этапы деплоя:

```
✓ Клонирование репозитория
✓ Восстановление зависимостей (dotnet restore)
✓ Публикация приложения (dotnet publish)
✓ Запуск приложения (dotnet out/DripCube.dll)
```

## Шаг 6: Проверка работоспособности

После успешного деплоя:

1. **Получите URL приложения:**
   - В Dashboard вы увидите URL вида: `https://dripcube-xxxx.onrender.com`

2. **Откройте приложение в браузере:**
   - Должна загрузиться главная страница
   
3. **Проверьте подключение к БД:**
   - Попробуйте авторизоваться с admin аккаунтом:
     - Логин: `admin`
     - Пароль: `admin123`

4. **Проверьте API:**
   - Swagger должен быть доступен по адресу (если включен в production)
   - Проверьте работу endpoint'ов

## Шаг 7: Настройка автоматического деплоя

✅ **Уже настроено!** В `render.yaml` включен `autoDeploy: true`

Это означает, что при каждом push в ветку `main`, Render автоматически:
1. Скачает новый код
2. Соберет проект
3. Задеплоит обновленную версию

## Решение типичных проблем

### Проблема: "Build failed"

**Решение:**
- Проверьте логи сборки в Render Dashboard
- Убедитесь, что проект компилируется локально: `dotnet build`
- Проверьте, что все зависимости в `DripCube.csproj` корректны

### Проблема: "Application crashed"

**Решение:**
1. Проверьте логи приложения в разделе "Logs"
2. Убедитесь, что все переменные окружения настроены правильно
3. Проверьте строку подключения к БД

### Проблема: "Cannot connect to database"

**Решение:**
1. Убедитесь, что переменная `DATABASE_URL` настроена правильно
2. Проверьте, что PostgreSQL сервис на Render активен
3. В настройках PostgreSQL на Render проверьте:
   - External Database URL
   - SSL settings

### Проблема: "Port already in use"

**Решение:**
- Убедитесь, что в `Program.cs` не захардкожен порт
- Переменная `ASPNETCORE_URLS` должна использовать `$PORT`

### Проблема: Cloudinary/Stripe не работают

**Решение:**
1. Проверьте, что API ключи настроены в переменных окружения
2. Убедитесь, что имена переменных совпадают с используемыми в коде
3. Для Cloudinary: проверьте формат настроек в `CloudinarySettings`

## Мониторинг и логи

### Просмотр логов

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в раздел "Logs"
3. Выберите тип логов:
   - **Deploy** - логи деплоя и сборки
   - **App** - логи работы приложения

### Полезные команды для отладки

В коде уже есть логирование:
```csharp
Console.WriteLine("--- ADMIN CREATED: Login: admin / Pass: admin123 ---");
```

Эти сообщения будут видны в логах приложения.

## Обновление приложения

Для обновления приложения:

1. Внесите изменения в код
2. Закоммитьте и запушьте в Git:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```
3. Render автоматически запустит новый деплой

### Ручной редеплой

Если нужно перезапустить без изменений кода:
1. В Render Dashboard откройте Web Service
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

## Масштабирование

Для Free плана Render предоставляет:
- 750 часов в месяц
- Автоматический sleep после 15 минут неактивности
- Пробуждение при первом запросе (может занять ~30 секунд)

Для постоянной работы рассмотрите платные планы.

## Дополнительные настройки

### Настройка кастомного домена

1. В Render Dashboard → ваш Web Service
2. Перейдите в "Settings" → "Custom Domain"
3. Добавьте ваш домен и настройте DNS записи

### Мониторинг производительности

Render предоставляет базовые метрики:
- CPU usage
- Memory usage
- Response time

Доступ: Dashboard → Web Service → "Metrics"

## Поддержка

- [Render Documentation](https://render.com/docs)
- [Render Community](https://community.render.com/)
- [ASP.NET Core Deployment Guide](https://learn.microsoft.com/aspnet/core/host-and-deploy/)

---

## Checklist перед деплоем

- [ ] Код закоммичен и запушен в Git
- [ ] Web Service создан на Render
- [ ] Все переменные окружения настроены
- [ ] База данных PostgreSQL доступна
- [ ] Cloudinary API ключи актуальны
- [ ] Stripe API ключи актуальны
- [ ] Проверен первый деплой
- [ ] Протестирована авторизация admin
- [ ] Настроен автоматический деплой

**Успешного деплоя! 🚀**
